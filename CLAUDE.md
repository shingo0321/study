# CLAUDE.md

This file provides guidance to Claude Code when working in this directory.

## What this is

Personal study notes, git repo (`shingo0321/study`). One folder per subject; today only `claude/` exists.

- `claude/` — Anthropic's official Claude Certification Program (4 exams, all via Pearson VUE). One folder per exam: `associate-foundations/`, `developer-foundations/`, `architect-foundations/`, `architect-professional/`. See `claude/README.md` for the exam list and each exam folder's own `README.md` for its domain breakdown. Each exam folder follows the same layout: `topics/<NN-domain-name>/notes.md` (one per official exam domain) + `practice-questions/README.md` (original practice questions, not real exam content).

## Notes

- This is a separate git repository from the parent `c:\Users\snows\Claude\` workspace (which is not itself a git repo).
- Content is written in Japanese.
